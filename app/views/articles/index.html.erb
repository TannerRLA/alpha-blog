<h1 align="center">Listing all articles</h1>

<% @articles.each do |article| %>
  <div class="row">
    <div class="col-sm-8 col-sm-offset-5">
      <div class="border border-2 border-dark rounded">
        <div class="article-title">
          <%= link_to article.title, article_path(article) %>
        </div>
        <div class="article-body">
          <%= truncate(article.description, lenght: 100) %>
        </div>
        <div class="article-actions">
          <%= link_to "Edit this article", edit_article_path(article), class: "btn btn-xs btn-sm btn-primary" %>
          <%= link_to "Delete this article", article_path(article), method: :delete, data: {confirm: "Are you sure you want to delete this article?"}, class: "btn btn-xs btn-sm btn-danger" %>
        </div>
      </div>
    </div>
  </div>
<% end %>
